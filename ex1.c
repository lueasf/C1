#include <stdio.h>
#include <stdlib.h>

int main(){
    int age = 19;
    float taille = 1.68;
    char note = 'A';
    printf("age = %d, taille = %f, note = %c\n", age, taille, note);
    return 0;
}